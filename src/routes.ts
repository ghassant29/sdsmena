class Routes {
  static readonly HOME = "/";
  static readonly PRODUCT_DETAIL = "/product/:productId";
  static readonly PRODUCTS = "/products"; // Add this line for the products route
}

export default Routes;
